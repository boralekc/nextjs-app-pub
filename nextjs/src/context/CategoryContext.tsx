// 'use client'
// import React, { createContext } from "react";
// import ItemArticle from "@/entities/ItemArticle";

// // Создаем контекст
// export const CategoryContext = createContext();

// // Создаем компонент-провайдер, который будет предоставлять значение контекста
// export const CategoryProvider = ({ children }) => {
//   // Установка начального значения контекста
//   const value = 'Привет';

//   return (
//     <CategoryContext.Provider value={value}>
//       {children}
//     </CategoryContext.Provider>
//   );
// };

// // В вашем Layout компоненте используйте CategoryProvider вокруг компонентов, которые должны иметь доступ к контексту.
